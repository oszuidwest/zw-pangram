---
name: JS Lint

on:
  push:
    branches: [main]
    paths:
      - '**/*.js'
      - '**/*.ts'
      - '**/*.css'
      - 'package.json'
      - 'package-lock.json'
      - 'biome.json'
      - '.github/workflows/lint-js.yml'
  pull_request:
    paths:
      - '**/*.js'
      - '**/*.ts'
      - '**/*.css'
      - 'package.json'
      - 'package-lock.json'
      - 'biome.json'
      - '.github/workflows/lint-js.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    uses: oszuidwest/.github-templates/.github/workflows/wp-js-ci.yml@v3
